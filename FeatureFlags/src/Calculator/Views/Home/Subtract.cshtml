@{
    ViewBag.Title = "Subtraction";
}

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <form id="calculatorForm" method="post">
                <div class="row">
                    <p>
                        <div class="form-group">
                            <input type="text" name="firstNumber" value="@ViewBag.firstNumber" />
                        </div>
                        <div class="form-group">
                            <input type="text" name="secondNumber" value="@ViewBag.secondNumber" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Calculate" />
                        </div>
                    </p>
                </div>

                <div class="row">
                    Result: <span>@ViewBag.Subtract</span>
                </div>
            </form>
        </div>
    </div>
</div>